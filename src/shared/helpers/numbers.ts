const moneyFormatter = new Intl.NumberFormat('en-US', {
  style: 'currency',
  currency: 'USD',
  minimumFractionDigits: 2,
  maximumFractionDigits: 2,
});

const flatFormatter = new Intl.NumberFormat('en-US');

export { moneyFormatter, flatFormatter }
